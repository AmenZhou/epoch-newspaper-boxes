<nav class="navbar navbar-static-top navbar-default" role="navigation">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
			<%= link_to(root_path, class:"navbar-brand") do %>
        <%= image_tag '/assets/logo.png'%>
      <% end %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      
		<ul class="nav navbar-nav navbar-right">
			<li><%= link_to "主页", root_path, data: { no_turbolink: true } %></li>
      <% if current_user && current_user.is_admin?%>
        <li class="dropdown">
          <a class="dropdown-toggle" data-toggle="dropdown" href="#">
            報紙發行統計表格
            <span class="caret"></span>
          </a>
          <ul class="dropdown-menu" role="menu">
            <li><%= link_to "報箱統計表", newspaper_boxes_path %>
            <li><%= link_to "手發統計表", newspaper_hands_path %>
          </ul>
        </li>
        <li><%= link_to "郵編統計圖表", zipcode_report_reports_path, data: { no_turbolink: true } %>
        <li class="dropdown">
          <a class="dropdown-toggle" data-toggle="dropdown" href="#">
            區統計圖表
            <span class="caret"></span>
          </a>
          <ul class="dropdown-menu" role="menu">
            <li><%= link_to "報箱統計表", report_reports_path(table_type: 'NewspaperBox'), data: { no_turbolink: true } %>
            <li><%= link_to "手發統計表", report_reports_path(table_type: 'NewspaperHand'), data: { no_turbolink: true } %>
          </ul>
        </li>
        <li><%= link_to "趨勢圖", histories_path%>
      <% end %>
			<% if user_signed_in? %>
				<li><%= link_to "注销", destroy_user_session_path, method: :delete %></li>
			<% else %>
				<li><%= link_to "登陆", new_user_session_path %></li>
			<% end %>
		</ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container -->
</nav>